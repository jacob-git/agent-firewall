# Contributing

Keep changes minimal and aligned with the core architecture:

`Intent -> Policy -> Decision -> Execution -> Ledger`

- Preserve the existing control boundary between AI output and execution
- Avoid adding UI, frameworks, or unnecessary abstractions
- Prefer clarity over cleverness
- Keep types explicit and naming consistent with the domain
